name: Deploy to SAP BTP

on:
  push:
    branches: [ "main" ]  # 当代码推送到 main 分支时自动触发
  workflow_dispatch:      # 允许你在 GitHub 页面上手动点击按钮触发 (方便测试)

jobs:
  deploy-to-sap:
    runs-on: ubuntu-latest
    steps:
      # 1. 下载你的代码
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. 安装 Cloud Foundry CLI (SAP 的命令行工具)
      - name: Install Cloud Foundry CLI
        run: |
          wget -q -O - https://packages.cloudfoundry.org/debian/cli.cloudfoundry.org.key | sudo apt-key add -
          echo "deb https://packages.cloudfoundry.org/debian stable main" | sudo tee /etc/apt/sources.list.d/cloudfoundry-cli.list
          sudo apt-get update
          sudo apt-get install cf8-cli

      # 3. 登录并设置目标空间 (使用我们在 Settings 里配置的密码)
      - name: Login to SAP BTP
        env:
          CF_API: ${{ secrets.CF_API }}
          CF_USER: ${{ secrets.CF_USERNAME }}
          CF_PASS: ${{ secrets.CF_PASSWORD }}
          CF_ORG: ${{ secrets.CF_ORG }}
          CF_SPACE: ${{ secrets.CF_SPACE }}
        run: |
          cf api "$CF_API"
          cf auth "$CF_USER" "$CF_PASS"
          cf target -o "$CF_ORG" -s "$CF_SPACE"

      # 4. 推送应用 (这一步会自动读取根目录的 manifest.yml)
      - name: Push App
        run: cf push
